services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: techform
      POSTGRES_PASSWORD: techform
      POSTGRES_DB: techform
    ports:
      - "5436:5432"
    volumes:
      - techform_pg:/var/lib/postgresql/data

volumes:
  techform_pg:
